import React from 'react';

const Landing = () => (
  <div>
    <h1>Welcome to our Gym App</h1>
    <p>Please sign in or create an account via the sign in link</p>
  </div>
);

export default Landing;